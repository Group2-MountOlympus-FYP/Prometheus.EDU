'use client';


import {useParams} from 'next/navigation';
import LeadGridPage from "@/components/LeadGridPage/page";

export default function TagPage() {
    const {tag} = useParams(); // 获取动态路由参数
    return 0
}